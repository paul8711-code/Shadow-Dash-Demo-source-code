// Create Event
timer = 0;          // Tracks the time
global.saved_time = 0;     // Stores the time when reset
global.timer_reset = false;
global.times = 0;
persistent = true;  // Keeps the timer persistent across rooms

